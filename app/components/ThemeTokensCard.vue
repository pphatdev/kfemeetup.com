<template>
  <div class="w-[367px] min-h-[357px] bg-primary-foreground p-6 rounded-[16px] flex flex-col gap-[14px] items-start shadow-md font-sans transition-colors duration-200">
    <!-- Primary Color Badge -->
    <div class="bg-primary text-primary-foreground text-xs font-semibold px-4 py-2 rounded-[10px] shadow-xs">
      Primary
    </div>

    <!-- Secondary Color Badge -->
    <div class="bg-secondary text-secondary-foreground text-xs font-semibold px-4 py-2 rounded-[10px] shadow-xs">
      Secondary
    </div>

    <!-- Background Color Badge -->
    <div class="bg-background text-foreground border border-neutral-200 text-xs font-semibold px-4 py-2 rounded-[10px] shadow-xs">
      Background
    </div>

    <!-- Primary-foreground Color Badge -->
    <div class="bg-primary-foreground text-primary border border-primary/20 text-xs font-semibold px-4 py-2 rounded-[10px] shadow-xs">
      Primary-foreground
    </div>

    <!-- Secondary-foreground Color Badge -->
    <div class="bg-secondary-foreground text-secondary border border-secondary/20 text-xs font-semibold px-4 py-2 rounded-[10px] shadow-xs">
      Secondary-foreground
    </div>

    <!-- Primary-linear Gradient Badge -->
    <div class="bg-primary-linear text-primary-foreground text-xs font-semibold px-4 py-2 rounded-[10px] shadow-xs mt-3">
      Primary-linear
    </div>
  </div>
</template>

<script setup lang="ts">
// ThemeTokensCard dynamically styled with the design system theme color tokens
</script>
